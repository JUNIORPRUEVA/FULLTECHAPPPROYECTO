# 🏗️ ARQUITECTURA DEL MÓDULO DE USUARIOS

## Diagrama General de Flujo

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         FULLTECH CRM - USUARIOS                         │
└─────────────────────────────────────────────────────────────────────────┘

                              USUARIO FINAL
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
            ┌──────────────┐              ┌──────────────┐
            │  MÓVIL FLUTTER            │ DESKTOP FLUTTER
            │  (Cards View)             │ (Table View)
            └──────────────┘             └──────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │    FLUTTER UI LAYER           │
                    ├───────────────────────────────┤
                    │ • users_list_page.dart        │
                    │ • user_form_page.dart         │
                    │ • user_detail_page.dart       │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │   RIVERPOD STATE MGT          │
                    ├───────────────────────────────┤
                    │ • usuariosListProvider        │
                    │ • usuarioDetailProvider       │
                    │ • usuarioFormProvider         │
                    │ • usuariosRepositoryProvider  │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │   REPOSITORY LAYER            │
                    ├───────────────────────────────┤
                    │ usuarios_repository.dart      │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │   DATASOURCE LAYER            │
                    ├───────────────────────────────┤
                    │ usuarios_remote_datasource.dart
                    │ (Uso de Dio para HTTP)        │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │    HTTP CLIENT (DIO)          │
                    └───────────────┬───────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
    ┌────────┐           ┌──────────────┐         ┌──────────────┐
    │ CRUD   │           │ UPLOADS      │         │ IA + PDFs    │
    │REST    │           │ Documentos   │         │              │
    │        │           │              │         │              │
    └────────┘           └──────────────┘         └──────────────┘
        │                       │                         │
        ▼                       ▼                         ▼
    
    API EXPRESS
    ┌───────────────────────────────────────────────────────────┐
    │                 BACKEND NODEJS/TYPESCRIPT                  │
    ├───────────────────────────────────────────────────────────┤
    │                                                            │
    │ ┌─────────────────────────────────────────────────────┐  │
    │ │            USUARIOS ROUTES                          │  │
       │ │  ├── GET    /api/users                              │  │
       │ │  ├── GET    /api/users/:id                          │  │
       │ │  ├── POST   /api/users                              │  │
       │ │  ├── PUT    /api/users/:id                          │  │
       │ │  ├── PATCH  /api/users/:id/block                    │  │
       │ │  ├── PATCH  /api/users/:id/unblock                  │  │
       │ │  ├── DELETE /api/users/:id                          │  │
       │ │  ├── POST   /api/users/ia/extraer-desde-cedula       │  │
    │ │  ├── POST   /api/uploads/users                      │  │
       │ │  ├── GET    /api/users/:id/profile-pdf              │  │
       │ │  └── GET    /api/users/:id/contract-pdf             │  │
    │ └─────────────────────────────────────────────────────┘  │
    │                           │                               │
    │          ┌────────────────┼────────────────┐              │
    │          ▼                ▼                ▼              │
    │    ┌──────────┐    ┌──────────┐    ┌──────────┐          │
    │    │CONTROLLER│    │ UPLOADS  │    │   PDF    │          │
    │    │          │    │          │    │          │          │
    │    │ CRUD +   │    │ MULTER   │    │PUPPETEER │          │
    │    │  BLOCK   │    │ Handler  │    │ HTML→PDF │          │
    │    └──────────┘    └──────────┘    └──────────┘          │
    │          │                ▼                ▼              │
    │          │           /uploads/users/    Temp PDFs        │
    │          │                                               │
    │    ┌──────────────────────────────────┐                 │
    │    │     AI SERVICE                   │                 │
    │    ├──────────────────────────────────┤                 │
    │    │ • extractDataFromCedula()        │                 │
    │    │ • Llama a OpenAI GPT-4 Vision    │                 │
    │    │ • Normaliza fechas               │                 │
    │    │ • Calcula edad                   │                 │
    │    └──────────┬───────────────────────┘                 │
    │              │                                           │
    │              ▼                                           │
    │         ┌──────────────┐                                │
    │         │ ZOD SCHEMAS  │                                │
    │         │ VALIDACIONES │                                │
    │         └──────────────┘                                │
    │              │                                           │
    │              ▼                                           │
    │    ┌──────────────────────────────────┐                 │
    │    │     PRISMA ORM                   │                 │
    │    │  Acceso a BD                     │                 │
    │    └──────────────────────────────────┘                 │
    └───────────────┬───────────────────────────────────────┘
                    │
    ┌───────────────▼───────────────┐
    │   PostgreSQL DB               │
    │ fulltechapp_sistem            │
    ├───────────────────────────────┤
    │ Tables:                       │
    │ • Usuario                     │
    │ • CompanySettings             │
    │ • Empresa                     │
    │ • (otros...)                  │
    └───────────────────────────────┘
    
    Archivos de usuario:
    └── uploads/users/
        ├── {uuid}_foto_perfil.jpg
        ├── {uuid}_cedula_frente.jpg
        └── {uuid}_carta_trabajo.pdf
```

---

## Flujo de Creación de Usuario

```
┌─────────────┐
│   USUARIO   │
│  (persona)  │
└──────┬──────┘
       │ Completa formulario
       ▼
┌──────────────────────────────────┐
│  USER FORM PAGE                  │
├──────────────────────────────────┤
│ • Inputs validados               │
│ • Foto capturada                 │
│ • Cédula capturada & IA          │
│ • Documentos subidos             │
└──────┬───────────────────────────┘
       │ Click "Crear Usuario"
       ▼
┌──────────────────────────────────┐
│  USUARIOS FORM NOTIFIER          │
│  (usuarioFormProvider)           │
├──────────────────────────────────┤
│ • createUsuario(data)            │
│ • setState(loading=true)         │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIOS REPOSITORY             │
├──────────────────────────────────┤
│ • createUsuario(data)            │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIOS DATASOURCE             │
│  (Dio HTTP Client)               │
├──────────────────────────────────┤
│ • POST /api/users                │
│ • Headers + body                 │
└──────┬───────────────────────────┘
       │ HTTP REQUEST
       ▼
┌──────────────────────────────────┐
│  EXPRESS SERVER                  │
│  PORT 3000                       │
├──────────────────────────────────┤
│ POST /api/users                  │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIOS CONTROLLER             │
│  createUsuario()                 │
├──────────────────────────────────┤
│ 1. Validar con Zod              │
│ 2. Hash password                │
│ 3. Calcular edad                │
│ 4. llenar posicion = rol        │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  PRISMA CLIENT                   │
│  usuario.create({})              │
└──────┬───────────────────────────┘
       │ INSERT
       ▼
┌──────────────────────────────────┐
│  PostgreSQL DB                   │
│  INSERT INTO "Usuario"...        │
│  RETURNING *                     │
└──────┬───────────────────────────┘
       │ HTTP RESPONSE (201)
       ▼
┌──────────────────────────────────┐
│  FRONTEND RECIBE                 │
│  usuario JSON                    │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIOS FORM NOTIFIER          │
│  setState(success)               │
│  setState(loading=false)         │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  UI SE RECONSTRUYE               │
│  SnackBar: "Usuario creado OK"   │
│  Navega a lista de usuarios      │
└──────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIOS LIST PROVIDER          │
│  refresh()                       │
│  Recarga lista                   │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  USUARIO APARECE EN TABLA        │
│  ✅ ÉXITO                        │
└──────────────────────────────────┘
```

---

## Flujo de Integración con IA (Cédula)

```
┌──────────────────────────────┐
│  USUARIO CAPTURA CÉDULA      │
│  Con cámara del dispositivo  │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  IMAGE PICKER                │
│  (package: image_picker)     │
│  Devuelve: File path         │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  USER FORM PAGE              │
│  Click "Capturar & Extraer"  │
│  Pasa path a datasource      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  USUARIOS DATASOURCE         │
│  extractCedulaData(path)     │
│  Lee archivo binario         │
└──────┬───────────────────────┘
       │ Convierte a Base64
       ▼
┌──────────────────────────────┐
│  HTTP POST                   │
│  /api/users/ia/extraer-desde-cedula     │
│  body: {imagenUrl: b64}      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  BACKEND                     │
│  POST /api/users/ia/extraer-desde-cedula│
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  USUARIOS CONTROLLER         │
│  extractCedulaData()         │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  AI IDENTITY SERVICE         │
│  extractDataFromCedula(img)  │
└──────┬───────────────────────┘
       │ Verifica API KEY
       │ Codifica imagen
       ▼
┌──────────────────────────────┐
│  OPENAI API CALL             │
│  POST /v1/chat/completions   │
│  Model: gpt-4-vision         │
│  +  Prompt especializado     │
│  +  Imagen Base64            │
└──────┬───────────────────────┘
       │ HTTP Request
       ▼
┌──────────────────────────────┐
│  OPENAI GPT-4 VISION         │
│  Procesa imagen              │
│  Lee cédula dominicana       │
│  Extrae:                     │
│  • nombre_completo           │
│  • cedula_numero             │
│  • fecha_nacimiento          │
│  • lugar_nacimiento          │
└──────┬───────────────────────┘
       │ Respuesta JSON
       ▼
┌──────────────────────────────┐
│  AI SERVICE                  │
│  Normaliza datos             │
│  Valida fechas               │
│  Retorna IdentityData        │
└──────┬───────────────────────┘
       │ JSON response
       ▼
┌──────────────────────────────┐
│  CONTROLLER DEVUELVE         │
│  HTTP 200                    │
│  {data: {...extraídos...}}   │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  FRONTEND RECIBE DATOS       │
│  FormNotifier actualiza      │
│  setState(datos)             │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  USER FORM PAGE              │
│  _nombreCtrl.text =          │
│    datos['nombre_completo']  │
│  _cedulaCtrl.text = ...      │
│  _fechaNacimiento = ...      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  UI SE ACTUALIZA             │
│  Campos prellenados          │
│  ✅ ÉXITO                    │
│  Usuario ve datos extraídos  │
│  Y puede guardar o editar    │
└──────────────────────────────┘
```

---

## Flujo de Descarga de PDF

```
┌─────────────────────────────┐
│  USUARIO EN DETALLE         │
│  Click "Descargar PDF Ficha"│
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  USER DETAIL NOTIFIER       │
│  downloadProfilePDF(id)     │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  USUARIOS DATASOURCE        │
│  Dio GET request            │
│  /api/users/:id/pdf         │
│  ResponseType: bytes        │
└──────┬──────────────────────┘
       │ Binary PDF
       ▼
┌─────────────────────────────┐
│  BACKEND                    │
│  GET /usuarios/:id/pdf      │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  PDF CONTROLLER             │
│  generateProfilePDF()       │
├─────────────────────────────┤
│ 1. Buscar usuario en BD     │
│ 2. Buscar CompanySettings   │
│ 3. Generar HTML+CSS         │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  HTML TEMPLATE              │
│  (Profesional con datos)    │
│  • Logo empresa             │
│  • Datos personales         │
│  • Foto usuario             │
│  • Datos laborales          │
│  • Firma                    │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  PUPPETEER (Headless Chrome)│
│  Abre navegador sin UI      │
│  Carga HTML                 │
│  Renderiza a PDF            │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  PDF GENERADO EN MEMORIA    │
│  (NO se almacena en disco)  │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  HTTP RESPONSE              │
│  Content-Type: application  │
│  /pdf                       │
│  Content-Disposition:       │
│  attachment; filename=...   │
│  Body: [PDF bytes]          │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  FRONTEND RECIBE BYTES      │
│  Trigger descarga           │
│  navegador guarda archivo   │
│  ~/Descargas/ficha_xxx.pdf  │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  ✅ PDF DESCARGADO          │
│  Usuario puede abrir/ver    │
│  en Adobe Reader, etc       │
└─────────────────────────────┘
```

---

## Estructura de Carpetas Final

```
fulltech_app_sistema/
│
├─ fulltech_api/
│  ├── src/
│  │   ├── services/
│  │   │   └── aiIdentityService.ts              ← ✅ NUEVO
│  │   └── modules/
│  │       ├── auth/
│  │       ├── usuarios/                         ← ✅ NUEVO
│  │       │   ├── usuarios.schema.ts
│  │       │   ├── usuarios.controller.ts
│  │       │   ├── uploads.controller.ts
│  │       │   ├── pdf.controller.ts
│  │       │   └── usuarios.routes.ts
│  │       └── (otros módulos)
│  ├── uploads/
│  │   └── users/                                 ← ✅ Documentos subidos
│  ├── prisma/
│  │   └── schema.prisma                         ← Actualizado
│  ├── package.json                              ← Actualizado deps
│  └── .env
│
├─ fulltech_app/
│  ├── lib/
│  │   └── features/
│  │       ├── usuarios/                         ← ✅ NUEVO
│  │       │   ├── data/
│  │       │   │   ├── datasources/
│  │       │   │   │   └── usuarios_remote_datasource.dart
│  │       │   │   └── repositories/
│  │       │   │       └── usuarios_repository.dart
│  │       │   ├── models/
│  │       │   │   └── usuario_model.dart
│  │       │   ├── state/
│  │       │   │   └── usuarios_controller.dart
│  │       │   ├── presentation/
│  │       │   │   ├── pages/
│  │       │   │   │   ├── users_list_page.dart
│  │       │   │   │   ├── user_form_page.dart
│  │       │   │   │   └── user_detail_page.dart
│  │       │   │   └── widgets/
│  │       │   └── usuarios_menu.dart
│  │       └── (otros features)
│  ├── pubspec.yaml                              ← Actualizado deps
│  └── main.dart                                 ← Agregar ruta
│
└─ DOCUMENTACIÓN:
    ├── MODULO_USUARIOS_COMPLETO.md              ← ✅ 20+ págs
    ├── CHECKLIST_USUARIOS.md                    ← ✅ 15+ págs
    ├── INICIO_RAPIDO_USUARIOS.md                ← ✅ 10 min setup
    ├── RESUMEN_USUARIOS.md                      ← ✅ Executive summary
    └── ARQUITECTURA_USUARIOS.md                 ← Este archivo
```

---

## Dependencias Agregadas

### Backend (Node.js)
```json
{
  "multer": "^1.4.5",           // Manejo de uploads
  "puppeteer": "^21.0.0",       // Generación PDFs
  "axios": "^1.6.0",            // HTTP calls a IA
  "bcrypt": "^5.1.0",           // Hash de passwords
  "uuid": "^9.0.0"              // IDs únicos para archivos
}
```

### Frontend (Flutter)
```yaml
image_picker: ^1.0.0            # Captura de cámara/galería
intl: ^0.19.0                   # Formato de números/fechas
flutter_riverpod: ^2.0.0        # State management
dio: ^5.0.0                     # HTTP client
json_annotation: ^4.8.0         # JSON serialization
```

---

## Métricas del Proyecto

```
BACKEND:
├── Líneas de código:        ~600 (6 archivos)
├── Métodos API:             13 endpoints
├── Tablas DB:               2 nuevas (Usuario, CompanySettings)
├── Servicios:               1 (IA)
├── Controladores:           3 (CRUD, Uploads, PDF)
└── Validaciones:            Zod schemas

FRONTEND:
├── Líneas de código:        ~1500 (7 archivos + 1 menu)
├── Pantallas:               3 (Lista, Formulario, Detalle)
├── State Notifiers:         3 (Lista, Detalle, Formulario)
├── Validadores:             10+ campos
├── Responsive:              Mobile + Desktop
└── Características:         Upload, IA, PDF download

DOCUMENTACIÓN:
├── Páginas totales:         60+ páginas
├── Archivos:                5 documentos
├── Diagramas:               Múltiples
└── Ejemplos:                +20 ejemplos funcionales
```

---

## Tecnologías Utilizadas

```
Backend:
✓ Node.js 18+
✓ TypeScript 5+
✓ Express.js
✓ Prisma ORM
✓ PostgreSQL
✓ Zod (validaciones)
✓ Puppeteer (PDFs)
✓ Multer (uploads)
✓ Bcrypt (seguridad)
✓ OpenAI API (IA)

Frontend:
✓ Flutter 3.x
✓ Dart 3.x
✓ Riverpod (state)
✓ Dio (HTTP)
✓ Image Picker
✓ Intl
✓ JSON Serializable

DevOps:
✓ Git
✓ npm/pub
✓ Prisma migrations
✓ Flutter build
```

---

**Documentación generada**: Enero 2024 | **Versión**: 1.0.0 | **Estado**: ✅ COMPLETO
