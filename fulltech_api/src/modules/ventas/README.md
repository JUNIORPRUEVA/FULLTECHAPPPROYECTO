# Ventas (módulo)

CRUD inicial de referencia.

- Protegido por JWT.
- `POST/PUT` requieren rol `admin` o `vendedor`.
- `DELETE` requiere rol `admin`.

> TODO: Ajustar reglas y estado según procesos reales.
