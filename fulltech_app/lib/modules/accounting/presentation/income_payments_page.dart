import 'package:flutter/material.dart';

import '../../../core/widgets/module_page.dart';

class IncomePaymentsPage extends StatelessWidget {
  const IncomePaymentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ModulePage(
      title: 'Income & Payments (Ingresos y Cobros)',
      child: Card(
        child: Center(
          child: Text('Coming soon'),
        ),
      ),
    );
  }
}
