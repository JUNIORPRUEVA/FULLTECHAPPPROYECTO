import 'package:flutter/material.dart';

class RrhhDetailScreen extends StatelessWidget {
  const RrhhDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RRHH')),
      body: const Padding(
        padding: EdgeInsets.all(16),
        child: Text('TODO: Detalle/edición de proceso RRHH.'),
      ),
    );
  }
}
