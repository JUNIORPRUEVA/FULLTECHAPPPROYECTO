import 'package:flutter/material.dart';

class GarantiaDetailScreen extends StatelessWidget {
  const GarantiaDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Garantía')),
      body: const Padding(
        padding: EdgeInsets.all(16),
        child: Text('TODO: Detalle/edición de caso de garantía.'),
      ),
    );
  }
}
